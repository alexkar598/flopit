query SubFeed($cursor: ID, $sub_name: String!) {
  __typename
  subByName(name: $sub_name) {
    id
    posts(after: $cursor) {
      edges {
        node {
          ...TopPostCard
        }
      }
      pageInfo {
        endCursor
        hasNextPage
      }
    }
  }
}
