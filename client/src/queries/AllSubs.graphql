query AllSubs {
  subs {
    edges {
      node {
        id
      }
    }
  }
}


query SubInformation {
  subByName(name: $SubName) {
    name
    description
    followers {
      totalCount
    }
  }
}
