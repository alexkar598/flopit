query SubInformation($sub_name: String!) {
  subByName(name: $sub_name) {
    id
    name
    description
    iconUrl
    bannerUrl
    isModerator
    followers {
      totalCount
    }
    posts {
      totalCount
    }
    isFollowing
  }
}
