query ListConversationMessages($conversation: ID!) {
  node(id: $conversation) {
    ... on Conversation {
      messages(last: 20) {
        pageInfo {
          endCursor
          hasNextPage
        }
        edges {
          node {
            ...Message
          }
        }
      }
    }
  }
}
